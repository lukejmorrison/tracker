# Nothing is required. The atlas is compiled TypeScript.

# Optional — bind a different port (default 8080)
# PORT=5173

# Optional — only if you are testing sign-in against a real Postgres
# DATABASE_URL=postgresql://user:pass@localhost:5432/tracker
